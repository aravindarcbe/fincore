# Copy to backend.hcl (gitignored) and fill in the values printed by
# infra/terraform-bootstrap's `terraform output backend_hcl`.
# Then: terraform init -backend-config=backend.hcl

bucket  = "REPLACE_ME"
key     = "fincore/terraform.tfstate"
region  = "ap-south-1"
encrypt = true
